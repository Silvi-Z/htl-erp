import type {ReactNode} from 'react';import styled from 'styled-components';
const Header=styled.header`display:flex;align-items:center;gap:16px;padding:16px 30px 14px;position:sticky;top:0;z-index:6;background:linear-gradient(180deg,${p=>p.theme.colors.paper} 74%,rgba(246,242,234,0))`;const Title=styled.div`font-family:${p=>p.theme.typography.display};font-size:22px;font-weight:700`;const Crumb=styled.div`font-size:12px;color:${p=>p.theme.colors.muted};margin-bottom:2px`;const Actions=styled.div`margin-left:auto;display:flex;align-items:center;gap:8px`;
export function PageHeader({title,crumb,actions}:{title:string;crumb?:string;actions?:ReactNode}){return <Header><div>{crumb&&<Crumb>{crumb}</Crumb>}<Title>{title}</Title></div>{actions&&<Actions>{actions}</Actions>}</Header>}
